{
  "name": "dongfangbai",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "check": "astro check",
    "test:market": "node --test tests/market-*.test.mjs",
    "test:market:coverage": "node --test --experimental-test-coverage tests/market-*.test.mjs",
    "deploy": "npm run build && wrangler deploy",
    "deploy:server": "bash deploy/deploy.sh",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.91.0"
  }
}
